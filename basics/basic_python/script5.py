# objects
# everything in python is an object
# objects have attributes and methods 
# can be access using the '.' syntax

age = -8
print(age.bit_length())
# print(age)
print(age.real)
print(age.imag)
print(age.__abs__())
print(age.__hash__(), end=' ')

name = 'josh'
print(name)

""" 
all objects in python as properties(attributes) and methods
e.g data types
"""